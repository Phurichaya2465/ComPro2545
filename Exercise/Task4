#4. จงเขียนโปรแกรมรับค่าตัวเลข 1 ตัว (h) จากคีย์บอร์ด จากนั้นแสดงผลตัวเลขตั้งแต่ 1 - n ในบันทัดเดียวกันด้วยคำสั่ง while loop 
n = int(input("Enter number : "))
while n == n:
    for n in range(1, n+1):
        print("{}".format(n), end=" ")
    break
